import {
    StyleSheet
} from "react-native";

const styles = StyleSheet.create({
    container: {
        // is empty
    },
    title: {
        textAlign: "center"
    },
    formContainer: {
        justifyContent: "center",
        alignItems: "center",
        flex: 1
    },
    redirectToRegisterContainer: {
        justifyContent: "center",
        flexDirection: "row",
        alignItems: "center"
    }
});
export default styles;
